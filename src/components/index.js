// 注册left导航栏为全局组件
// import leftNavigation from './leftNavigation/index'

// 注册head导航栏为全局组件
// import headNavigation from './headNavigation/index'

export default (Vue) => {
    // Vue.component('leftNavigation', leftNavigation)
    // Vue.component('headNavigation', headNavigation)
}
